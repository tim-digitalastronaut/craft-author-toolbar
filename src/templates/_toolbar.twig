{# ======================= #}
{# PROPERTIES              #}
{# ======================= #}
{% set pageUrl = craft.app.request.absoluteUrl %}
{% set isProduct = false %}

{% if entry %}
    {% if entry.className == 'craft\\commerce\\elements\\Product' %}
        {% set isProduct = true %}
    {% endif %}

    {% set pageUrl = entry.url|default("") %}
    {% set pageTitle = entry.title|default("") %}
    {% set cpEditUrl = entry.cpEditUrl|default("") %}
    {% set pageStatus = entry.status|default("") %}
    {% set updatedAt = 
        "Last edited "|t('author-toolbar') ~
        entry.dateUpdated|date("d F Y") ~ 
        " om "|t('author-toolbar') ~ 
        entry.dateUpdated|date("h:s")|default("") %}
{% else %}
    {% set pageTitle = "Custom route"|t %}
    {% set cpEditUrl = cpUrl("dashboard") %}
    {% set pageStatus = "live" %}
    {% set updatedAt = null %}
{% endif %}

{# ======================= #}
{# TEMPLATE                #}
{# ======================= #}
{% if currentUser and entry and currentUser.can("author-toolbar:access-toolbar") %}
    <author-toolbar id="cat-toolbar" class="cat-toolbar {{ settings.toolbarCollapsedByDefault ? "hidden" : "" }}">
         <div class="cat-toolbar-toggle">
            <button id="author-toolbar-toggle-button" class="cat-toolbar-toggle-button cat-typo-body-sm">
                <span>{{ "Hide toolbar"|t("author-toolbar") }}</span>

                {{ svg("@digitalastronaut/craftauthortoolbar/web/icons/chevron-down.svg") }}
            </button>
        </div>
        
        <div class="cat-toolbar-main">
            {% if 
                settings.startMenuEnabled and
                currentUser.can("author-toolbar:access-start-menu")  
            %}
                {% include "author-toolbar/components/chunks/_startMenu" %}
            {% endif %}

             {% if 
                settings.actionButtonsEnabled and
                currentUser.can("author-toolbar:access-action-buttons")  
            %}
                {% include "author-toolbar/components/chunks/_actionButtons" %}
            {% endif %}
            
            {% if 
                settings.globalSearchEnabled and
                currentUser.can("author-toolbar:access-global-search")  
            %}
                {% include "author-toolbar/components/chunks/_globalSearch" %}
            {% endif %}

            {% if 
                settings.seoMenuEnabled and
                currentUser.can("author-toolbar:access-seo-menu")  
            %}
                {% include "author-toolbar/components/chunks/_seoMenu" %}
            {% endif %}

            {% if 
                settings.helpMenuEnabled and
                currentUser.can("author-toolbar:access-help-menu")  
            %}
                {% include "author-toolbar/components/chunks/_helpMenu" %}
            {% endif %}

            {% if currentUser.can("author-toolbar:access-custom-chunks") %}
                {% for widget in settings.customChunks %}
                    {% include widget["path"] %}
                {% endfor %}
            {% endif %}
        </div>

         {% if settings.contextBarEnabled and currentUser.can("author-toolbar:access-context-bar") %}
            <div class="cat-toolbar-bottom">
                <div class="cat-toolbar-location-info">
                    {{ svg("@digitalastronaut/craftauthortoolbar/web/icons/globe.svg") }}

                    <p class="cat-typo-body-sm">
                        <span class="cat-semi-bold">{{ pageTitle }}</span>
                        <span>{{ pageUrl }}</span>
                    </p>

                    <button id="author-toolbar-copy-page-url-button" class="cat-copy-to-clipboard">
                        {{ svg("@digitalastronaut/craftauthortoolbar/web/icons/copy.svg") }}
                    </button>

                    <div class="cat-page-status {{ pageStatus }}">
                        <div class="cat-page-status-icon"></div>

                        <p class="cat-typo-body-sm cat-uppercase">{{ pageStatus }}</p>
                    </div>
                </div>

                <div class="cat-toolbar-bottom-extra">
                    <p class="cat-typo-body-sm">{{ updatedAt }}</p>
                </div>
            </div>
        {% endif %}

         <ul class="cat-toolbar-warnings-list">
            <li 
                id="cat-environment-message" 
                class="cat-toolbar-warnings-list-item {{ settings.environmentMessageEnabled ? "" : "hidden" }}"
            >
                {{ svg("@digitalastronaut/craftauthortoolbar/web/icons/triangle-exclamation.svg") }}

                <p class="cat-typo-body-md">
                    {{ "You are currently viewing the" }}
                    {{ getenv('ENVIRONMENT') ?: getenv('CRAFT_ENVIRONMENT') }}
                    {{ "environment" }}

                    <a id="cat-hide-environment-message-button">
                        {{ "Don't show for this session"|t('author-toolbar') }}
                    </a>
                </p>
            </li>
        </ul>
    </author-toolbar>
{% endif %}

